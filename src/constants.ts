// Application constants

/**
 * Refresh interval for game timer updates (in milliseconds)
 * Default: 1000ms (1 second)
 */
export const TIMER_REFRESH_INTERVAL = 1000;

/**
 * Time conversion ratio: real minutes to game hours
 * 5 real minutes = 1 game hour
 */
export const REAL_TO_GAME_RATIO = 5;
